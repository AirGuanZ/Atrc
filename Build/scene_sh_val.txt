# Renderer
renderer = {
    type        = PathTracingRenderer;
    workerCount = -1;
    spp         = 100;
    integrator  = {
        type     = PathTracer;
        maxDepth = 10;
    };
};

# Image properties
output = { filename = "./Build/Output.png"; width = 600; height = 900; };

# Progress reporter
reporter = { type = Default; };

# Camera
camera = {
    type = Perspective;
    aspectRatio = 0.666666;
    eye  = (-7, 0, 0);
    dst  = (0, 0, 0);
    up   = (0, 0, 1);
    FOVz = Deg(28);
};

# Entity list
entities = ({
    type = GeometricEntity;
    geometry = {
        type = TriangleBVH;
        path = "./Assets/Statue_Bressant_1M_Poly.obj";
        transform = (Translate(0.0, 0.02, -0.945), RotateZ(Deg(-90)), Scale(0.0050));
    };
    material = {
        type = DiffuseMaterial;
        albedo = (0.8);
    };
});

# Light list
lights = (
{
    type = SphereEnvironmentLight;
    tex = "./Assets/RightLight.png";
}
);

CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

PROJECT(RASTERIZER)

FILE(GLOB_RECURSE RASTERIZER_SRC
		"${PROJECT_SOURCE_DIR}/src/*.cpp"
		"${PROJECT_SOURCE_DIR}/src/*.h"
		"${PROJECT_SOURCE_DIR}/include/agz/rasterizer/*.h"
		"${PROJECT_SOURCE_DIR}/include/agz/rasterizer/*.inl")

SET(Rasterizer_INCLUDE_DIRS "${PROJECT_SOURCE_DIR}/include/" "${AGZCommon_INCLUDE_DIRS}" "${AGZUtils_INCLUDE_DIRS}" "${MISC_INCLUDE_DIRS}" CACHE STRING "")
SET(Rasterizer_LIBRARIES AGZUtils ${STDFS_LD_LIB} CACHE STRING "")
